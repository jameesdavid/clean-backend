{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Debug NestJS",
      "request": "launch",
      "command": "yarn start:debug",
      "cwd": "${workspaceFolder}",
    }
  ]
}
